/* Généré automatiquement par Netlify à chaque déploiement (voir la
   commande de build dans netlify.toml). NE PAS MODIFIER À LA MAIN :
   ce fichier est entièrement réécrit à chaque mise en ligne, avec la
   date réelle de ce déploiement.

   La valeur ci-dessous n'est qu'un repli de secours, utilisé pour un
   aperçu local avant le tout premier déploiement Netlify avec ce
   mécanisme (ou si le fichier venait à manquer pour une raison ou une
   autre) : main.js sait s'en passer proprement le cas échéant. */
window.BUILD_DATE = "2026-08-09";
